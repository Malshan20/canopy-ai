fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
# Needed for pydantic's EmailStr (app/schemas/contact.py) — validates
# the email address on the public contact form at the API boundary,
# rather than accepting an unvalidated string with no auth gate to
# otherwise catch garbage input.
email-validator==2.2.0
python-multipart==0.0.20
python-dotenv==1.0.1
httpx==0.28.1
pypdf==5.1.0
PyMuPDF==1.25.1
tenacity==9.0.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
PyJWT[crypto]==2.10.1
pycountry==26.2.16
